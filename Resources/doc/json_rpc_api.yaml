json_rpc_api:
    handlers:
        main:
            path: /api
            host: ~
            serializer: ~
            logger: ~
            error_handler: ~
        extra:
            path: /auth
            host: ~
            serializer: ~
            logger: ~
            error_handler: ~
